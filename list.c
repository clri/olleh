/* creates linked list of chars or linked lists.
used zentut.com's linked list tutorial to implement. */

#define NULL 0

struct charnode{
    char data;
    struct charnode *next;
};

//typedef struct listnode{

int main() {
    struct charnode c = {'a', NULL};
}
