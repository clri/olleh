//test-foreach-pos: testing foreach loops


foreach x ['a','b','c']:
        print x;
end;

/*
map<String> m = fresh map(String);
foreach y m: //should have no visible output
        print y;
end;

m.set("example", 5);
m.set("another example", 0);
foreach z m:
        print z + " " + m.get(z);
end;*/
