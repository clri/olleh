/*
 *  Positive: testing foreach
 */

Board brd = fresh Board(1,5);

foreach x brd.subStrings():
    if (checkWord(x)):
    print pltIsGreat;
    end;
end;

int turns = 5;
for (turns):
	print tve;
end;
